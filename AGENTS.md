# Agent Instructions

Before creating a pull request (always create a new branch from `main` first), verify:

1. `npm test` passes
2. New/modified code has corresponding tests
3. README/docs updated if behaviour changed
4. No commented-out code or `console.log` leftovers

Match existing code style (named exports, no semicolons, vitest). Prefer existing libraries.
